# Memory

Claude Code can remember facts across sessions using a memory file.

## Sharing memory between projects

A memory file at the user level, rather than inside one project, is read by
every project you open, so a fact recorded there is available everywhere.
