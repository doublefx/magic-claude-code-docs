name: Create Release

on:
  push:
    branches:
      - main
    paths:
      - 'plugin/.claude-plugin/plugin.json'
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.version.outputs.tag }}

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0 # Get all history for changelog

    - name: Extract version from plugin.json
      id: version
      run: |
        VERSION=$(jq -r '.version' plugin/.claude-plugin/plugin.json)
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "tag=v$VERSION" >> $GITHUB_OUTPUT
        echo "Extracted version: $VERSION"

    - name: Check if release exists
      id: check_release
      run: |
        if gh release view "${{ steps.version.outputs.tag }}" >/dev/null 2>&1; then
          echo "exists=true" >> $GITHUB_OUTPUT
          echo "Release ${{ steps.version.outputs.tag }} already exists"
        else
          echo "exists=false" >> $GITHUB_OUTPUT
          echo "Release ${{ steps.version.outputs.tag }} does not exist"
        fi
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Create release
      if: steps.check_release.outputs.exists == 'false'
      run: |
        gh release create "${{ steps.version.outputs.tag }}" \
          --title "Claude Code Docs ${{ steps.version.outputs.tag }}" \
          --generate-notes \
          --notes "
        ## Claude Code Documentation Mirror ${{ steps.version.outputs.tag }}

        This release includes the latest updates to the Claude Code documentation mirror.

        ### Installation

        \`\`\`bash
        claude plugin marketplace add https://github.com/doublefx/magic-claude-code-docs
        claude plugin install magic-claude-docs
        \`\`\`

        ### What's Changed (Auto-generated)
        "
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Release already exists
      if: steps.check_release.outputs.exists == 'true'
      run: |
        echo "Release ${{ steps.version.outputs.tag }} already exists, skipping creation"

  # Search embeddings for this release (reusable workflow; a release created
  # with GITHUB_TOKEN never fires other workflows' `on: release`, so it is
  # called from here, right after the release step).
  embeddings:
    needs: release
    if: needs.release.outputs.tag != ''
    uses: ./.github/workflows/embeddings.yml
    with:
      tag: ${{ needs.release.outputs.tag }}
    permissions:
      contents: write
